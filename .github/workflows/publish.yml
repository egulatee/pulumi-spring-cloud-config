# ⚠️ DEPRECATED: This workflow is being replaced by release.yml with semantic-release automation
# This workflow will be removed in a future release once semantic-release is fully validated
# See Issue #46 for details: https://github.com/egulatee/pulumi-spring-cloud-config/issues/46

name: Publish to NPM (DEPRECATED)

on:
  # Disabled - use release.yml instead
  workflow_dispatch: # Manual trigger only for emergency use

jobs:
  publish:
    name: Publish to NPM
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Publish to NPM
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
