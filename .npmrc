# NPM Configuration for @egulatee/pulumi-spring-cloud-config

# Access level for scoped package
access=public

# Save exact versions in package.json
save-exact=true

# Use npm registry
registry=https://registry.npmjs.org/

# For publishing (requires NPM_TOKEN environment variable in CI)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
